export interface TileMapService {
  TileMaps: [
    {
      TileMap: [
        {
          $: {
            title: string
            srs: string
            profile: string
            href: string
          }
        }
      ]
    }
  ]
}